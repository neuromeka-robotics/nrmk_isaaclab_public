isaaclab_task: "Indy-Deploy"

communication:
  topic_namespace: "indy_sim"
  zenoh: null

simulation:
  real_time: true
  max_steps: null

commands:
  - topic: "joint_command"
    dtype: "float32"
    action_slice: [0, 6]

observations:
  root: "obs"
